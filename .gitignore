node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Firebase client config is NOT secret - it runs in the browser
# Security is enforced by Firestore Rules, not by hiding this file
# firebase-applet-config.json  <-- intentionally tracked in git
